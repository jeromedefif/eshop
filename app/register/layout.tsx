import React from 'react';

export const metadata = {
  title: 'Registrace | VINARIA s.r.o.',
  description: 'Vytvoření nového účtu',
}

export default function RegisterLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>;
}
